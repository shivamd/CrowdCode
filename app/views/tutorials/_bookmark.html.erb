<% if current_user && !tutorial.bookmarked_by?(current_user) %>
	<%= render 'shared/create_bookmark', tutorial: tutorial %>
<% else %>
	<%= render 'shared/delete_bookmark', tutorial: tutorial %>
<% end %>